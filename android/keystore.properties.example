# Copy this file to android/keystore.properties. Do not commit the copied file.
# Paths are resolved relative to the android directory.
storeFile=../keys/shiftfeel-release.keystore
storePassword=replace-with-a-secret
keyAlias=shiftfeel
keyPassword=replace-with-a-secret
